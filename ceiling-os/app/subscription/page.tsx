"use client";
import{useEffect,useState}from"react";import Link from"next/link";import"./subscription.css";
type S={plan:string;status:string;current_period_end?:string|null};
const plans=[['free','FREE','До 10 об’єктів','Базовий конструктор'],['pro','PRO','Безлімітні об’єкти','Кошториси, фінанси, документи'],['business','BUSINESS','Команда + склад','Виробництво, зарплата, фінанси'],['enterprise','ENTERPRISE','Для мереж','Філії, склади, розширені права']];
export default function SubscriptionPage(){const[s,setS]=useState<S|null>(null);const[err,setErr]=useState('');useEffect(()=>{fetch('/api/subscription').then(async r=>{const d=await r.json();if(!r.ok)throw new Error(d.error);setS(d.subscription)}).catch(e=>setErr(e.message))},[]);return <main className="subscription"><Link href="/">← На головну</Link><h1>Підписка СТЕЛЯ OS</h1><p className="muted">Функції системи відкриваються відповідно до тарифу.</p>{err&&<div className="error">{err}</div>}<div className="plans">{plans.map(([id,name,desc,extra])=><article className={s?.plan===id?'plan active':'plan'} key={id}><div className="eyebrow">{name}</div><h2>{desc}</h2><p>{extra}</p>{s?.plan===id&&<strong>Поточний тариф · {s.status}</strong>}</article>)}</div></main>}
