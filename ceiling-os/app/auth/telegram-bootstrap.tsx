"use client";import{useEffect}from"react";import{hydrateAuth,type UserProfile,type Company}from"../../lib/auth-model";
export function TelegramBootstrap(){useEffect(()=>{let cancelled=false;async function run(){const w=window as typeof window&{Telegram?:{WebApp?:{ready?:()=>void;initData?:string}}};const web=w.Telegram?.WebApp;web?.ready?.();if(!web?.initData)return;try{const r=await fetch("/api/auth/telegram",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({initData:web.initData})});const data=await r.json();if(!cancelled&&data.ok&&data.user&&data.company)hydrateAuth(data.user as UserProfile,data.company as Company);window.dispatchEvent(new Event("stelya-auth-ready"))}catch{}}void run();return()=>{cancelled=true}},[]);return null}
