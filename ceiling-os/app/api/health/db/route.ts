import {NextResponse}from"next/server";import{query}from"../../../../db/client";export const runtime="nodejs";export async function GET(){try{const r=await query<{now:string}>("select now()::text as now");return NextResponse.json({ok:true,database:"postgresql",now:r.rows[0]?.now??null})}catch(e){return NextResponse.json({ok:false,error:e instanceof Error?e.message:"Database unavailable"},{status:503})}}
