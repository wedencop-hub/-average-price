"use client";
import Link from"next/link";import{useEffect,useState}from"react";import{getEstimate}from"../../../../lib/estimate-store";import{costItemsFromEstimate,calculateInternalCost,type InternalCost}from"../../../../lib/nomenclature-model";import{getInternalCost,saveInternalCost}from"../../../../lib/internal-cost-store";import{getPayroll,type PayrollEntry}from"../../../../lib/payroll-store";import{getObjectConsumption}from"../../../../lib/warehouse-store";import{consumptionTotal,type StockMovement}from"../../../../lib/warehouse-model";import"./finance.css";
export default function FinancePage({params}:{params:Promise<{id:string}>}){const[id,setId]=useState("");const[cost,setCost]=useState<InternalCost|null>(null);const[revenue,setRevenue]=useState(0);const[install,setInstall]=useState(0);const[delivery,setDelivery]=useState(0);const[other,setOther]=useState(0);const[payroll,setPayroll]=useState<PayrollEntry[]>([]);const[movements,setMovements]=useState<StockMovement[]>([]);useEffect(()=>{params.then(async({id})=>{setId(id);const[estimate,saved,entries,warehouse]=await Promise.all([getEstimate(id),getInternalCost(id),getPayroll(id),getObjectConsumption(id)]);setPayroll(entries);setMovements(warehouse);const r=saved?.revenue??estimate?.total??0;const confirmed=entries.filter(e=>e.status!=="draft").reduce((s,e)=>s+e.amount,0);const labor=confirmed||saved?.installationCost||0;setRevenue(r);setInstall(labor);setDelivery(saved?.deliveryCost??0);setOther(saved?.otherCost??0);const base=costItemsFromEstimate(estimate?.items??[]);const material=warehouse.length?warehouse.map(m=>({id:m.id,sku:m.sku,name:m.name,unit:m.unit,quantity:Math.abs(m.quantity),unitCost:m.unitCost,total:Math.abs(m.quantity)*m.unitCost})):base;setCost(calculateInternalCost(id,material,r,labor,saved?.deliveryCost??0,saved?.otherCost??0))})},[params]);function recalc(i=install,d=delivery,o=other){if(cost)setCost(calculateInternalCost(id,cost.items,revenue,i,d,o))}async function save(){if(cost)await saveInternalCost(cost)}if(!cost)return <main className="finance"><Link href={`/objects/${id}`}>← До об’єкта</Link><h1>Фінанси об’єкта</h1><p>Спочатку створіть клієнтський кошторис.</p></main>;const issuedCost=consumptionTotal(movements);const materialCost=movements.length?issuedCost:cost.materialCost;const totalCost=materialCost+install+delivery+other;const profit=revenue-totalCost;const margin=revenue?profit/revenue*100:0;return <main className="finance"><Link href={`/objects/${id}`}>← До об’єкта</Link><header><div className="eyebrow">ВНУТРІШНІ ФІНАНСИ</div><h1>Собівартість та прибуток</h1></header><section className="metrics"><article><span>Виручка</span><strong>{revenue.toLocaleString("uk-UA")} ₴</strong></article><article><span>Фактичні матеріали</span><strong>{materialCost.toLocaleString("uk-UA")} ₴</strong></article><article><span>Собівартість</span><strong>{totalCost.toLocaleString("uk-UA")} ₴</strong></article><article><span>Прибуток</span><strong>{profit.toLocaleString("uk-UA")} ₴</strong><small>Маржа {margin.toFixed(1)}%</small></article></section><section className="panel"><h2>Матеріали</h2>{movements.length?movements.map(m=><div className="line" key={m.id}><span>{m.name} · {Math.abs(m.quantity)} {m.unit}</span><b>{(Math.abs(m.quantity)*m.unitCost).toLocaleString("uk-UA")} ₴</b></div>):cost.items.map(item=><div className="line" key={item.sku}><span>{item.name}</span><span>{item.quantity} {item.unit} × {item.unitCost} ₴ = <b>{item.total.toLocaleString("uk-UA")} ₴</b></span></div>)}</section><section className="panel"><h2>Зарплата монтажників</h2>{payroll.length?<>{payroll.map(p=><div className="line" key={p.id}><span>{p.installerName} · {p.status}</span><b>{p.amount.toLocaleString("uk-UA")} ₴</b></div>)}<div className="line"><b>Разом</b><b>{install.toLocaleString("uk-UA")} ₴</b></div></>:<p>Нарахувань ще немає.</p>}<label>Коригування зарплати<input type="number" value={install} onChange={e=>{const v=Math.max(0,Number(e.target.value)||0);setInstall(v);recalc(v)}} /></label></section><section className="panel"><h2>Інші витрати</h2><label>Доставка<input type="number" value={delivery} onChange={e=>{const v=Math.max(0,Number(e.target.value)||0);setDelivery(v);recalc(install,v,other)}} /></label><label>Інші витрати<input type="number" value={other} onChange={e=>{const v=Math.max(0,Number(e.target.value)||0);setOther(v);recalc(install,delivery,v)}} /></label><button className="primary" onClick={save}>Зберегти фінанси</button></section></main>}
