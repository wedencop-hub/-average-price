"use client";
import Link from"next/link";import{useEffect,useState}from"react";import{getLocalCompany}from"../../../lib/auth-model";import{listClients,listObjects,saveObject,type Client,type ObjectRecord,type ObjectStatus}from"../../../lib/crm-model";import{apiUpdateObjectStatus}from"../../../lib/crm-api";import{getOffline}from"../../../lib/offline-store";import type{CeilingDrawing}from"../../../lib/ceiling-model";import"./object-detail.css";
const labels:Record<ObjectStatus,string>={lead:"Лід",measurement:"Замір",estimate:"Кошторис",contract:"Договір",deposit_paid:"Аванс",production:"Виробництво",ready:"Готово",delivery:"Доставка",installation:"Монтаж",completed:"Завершено",cancelled:"Скасовано"};type Stored={drawing?:CeilingDrawing};
export default function ObjectDetail({params}:{params:Promise<{id:string}>}){const[id,setId]=useState(""),[companyId,setCompanyId]=useState(""),[object,setObject]=useState<ObjectRecord|null>(null),[client,setClient]=useState<Client|null>(null),[drawing,setDrawing]=useState<CeilingDrawing|null>(null);useEffect(()=>{setCompanyId(getLocalCompany()?.id??"")},[]);useEffect(()=>{if(!companyId)return;params.then(({id:routeId})=>{setId(routeId);const found=listObjects(companyId).find(x=>x.id===routeId);if(found){setObject(found);setClient(listClients(companyId).find(x=>x.id===found.clientId)??null);getOffline<Stored>(`object:${routeId}`).then(r=>setDrawing(r?.payload?.drawing??null)).catch(()=>{})}})},[params,companyId]);if(!object)return <main className="object-detail"><p>Об’єкт не знайдено.</p><Link href="/objects">← До об’єктів</Link></main>;async function changeStatus(status:ObjectStatus){try{await apiUpdateObjectStatus(object.id,status)}catch{}const next={...object,status,updatedAt:new Date().toISOString()};saveObject(next);setObject(next)}return <main className="object-detail"><Link href="/objects">← До об’єктів</Link><header><div className="eyebrow">ОБ’ЄКТ</div><h1>{object.title}</h1><p>{object.address}</p></header><section className="detail-grid"><article className="panel"><h2>Клієнт</h2><strong>{client?.name??"—"}</strong><p>{client?.phone??"Телефон не вказано"}</p></article><article className="panel"><h2>Статус</h2><select value={object.status} onChange={e=>void changeStatus(e.target.value as ObjectStatus)}>{Object.entries(labels).map(([key,label])=><option key={key} value={key}>{label}</option>)}</select></article></section><section className="panel workflow"><h2>Робота з об’єктом</h2><div className="workflow-actions"><Link className="primary-action" href={`/constructor?object=${encodeURIComponent(id)}`}>📐 {drawing?"Відкрити замір":"Створити замір"}</Link>{drawing?<Link className="primary-action" href={`/objects/${encodeURIComponent(id)}/estimate`}>🧾 Відкрити кошторис</Link>:<button disabled>🧾 Кошторис — після заміру</button>}{drawing?<Link className="primary-action" href={`/objects/${encodeURIComponent(id)}/production`}>🏭 Виробниче завдання</Link>:<button disabled>🏭 Виробництво — після кошторису</button>}<Link className="primary-action" href={`/objects/${encodeURIComponent(id)}/installation`}>👷 Монтаж і зарплата</Link><Link className="primary-action" href={`/objects/${encodeURIComponent(id)}/warehouse`}>📦 Матеріали / склад</Link><Link className="primary-action" href={`/objects/${encodeURIComponent(id)}/finance`}>💰 Фінанси / прибуток</Link></div>{drawing?<div className="drawing-status">Замір збережено · {drawing.walls.length} стін · {drawing.lights.length} світильників</div>:<p className="empty">Поки немає заміру. Відкрийте конструктор.</p>}</section></main>}
